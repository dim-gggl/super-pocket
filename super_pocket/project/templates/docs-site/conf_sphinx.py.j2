# Configuration file for the Sphinx documentation builder.
#
# For the full list of built-in configuration values, see the documentation:
# https://www.sphinx-doc.org/en/master/usage/configuration.html

import os
import sys
sys.path.insert(0, os.path.abspath('../..'))

# -- Project information -----------------------------------------------------
# https://www.sphinx-doc.org/en/master/usage/configuration.html#project-information

project = '{{ project_name }}'
copyright = '2025, {{ project_name }} Team'
author = '{{ project_name }} Team'
release = '0.1.0'

# -- General configuration ---------------------------------------------------
# https://www.sphinx-doc.org/en/master/usage/configuration.html#general-configuration

extensions = [
    'sphinx.ext.autodoc',
    'sphinx.ext.napoleon',
    'sphinx.ext.viewcode',
{%- if features.api_docs %}
    'sphinx.ext.autosummary',
{%- endif %}
{%- if features.i18n %}
    'sphinx.ext.intersphinx',
{%- endif %}
]

{% if features.api_docs -%}
autosummary_generate = True
autodoc_default_options = {
    'members': True,
    'member-order': 'bysource',
    'special-members': '__init__',
    'undoc-members': True,
    'exclude-members': '__weakref__'
}
{% endif %}

templates_path = ['_templates']
exclude_patterns = []

{% if features.i18n -%}
# Internationalization
language = 'en'
locale_dirs = ['../locales/']
gettext_compact = False
{% endif %}

# -- Options for HTML output -------------------------------------------------
# https://www.sphinx-doc.org/en/master/usage/configuration.html#options-for-html-output

{% if tool_choices.theme == 'readthedocs' -%}
html_theme = 'sphinx_rtd_theme'
html_theme_options = {
    'navigation_depth': 4,
    'collapse_navigation': False,
    'sticky_navigation': True,
    'includehidden': True,
    'titles_only': False
}
{%- elif tool_choices.theme == 'book' -%}
html_theme = 'sphinx_book_theme'
html_theme_options = {
    'repository_url': 'https://github.com/yourusername/{{ project_name }}',
    'use_repository_button': True,
    'use_issues_button': True,
    'use_edit_page_button': True,
}
{%- elif tool_choices.theme == 'alabaster' -%}
html_theme = 'alabaster'
html_theme_options = {
    'description': '{{ description }}',
    'fixed_sidebar': True,
}
{%- else -%}
html_theme = 'alabaster'
{%- endif %}

html_static_path = ['_static']

{% if features.search -%}
# Search functionality
html_search_language = 'en'
{%- endif %}

{% if features.versioning -%}
# Version support
html_context = {
    'display_github': True,
    'github_user': 'yourusername',
    'github_repo': '{{ project_name }}',
    'github_version': 'main',
    'conf_py_path': '/docs/source/',
}
{%- endif %}

[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "{{ description }}"
requires-python = "{{ python_version }}"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

Usage Guide
===========

This guide provides detailed examples of how to use {{ project_name }}.

Basic Usage
-----------

Start with the basics::

    import {{ project_name }}

    # Your example code here
    result = {{ project_name }}.example_function()

Advanced Features
-----------------

Explore more advanced capabilities:

Configuration
~~~~~~~~~~~~~

Configure {{ project_name }} for your needs::

    # Configuration example
    config = {
        'option1': 'value1',
        'option2': 'value2',
    }

Error Handling
~~~~~~~~~~~~~~

Handle errors gracefully::

    try:
        result = {{ project_name }}.example_function()
    except {{ project_name }}.ExampleError as e:
        print(f"Error: {e}")

Best Practices
--------------

Follow these best practices when using {{ project_name }}:

1. Always validate your input data
2. Use context managers when available
3. Handle exceptions appropriately
4. Check the return values

Common Patterns
---------------

Here are some common usage patterns:

Pattern 1: Basic Workflow
~~~~~~~~~~~~~~~~~~~~~~~~~~

::

    # Step 1: Initialize
    obj = {{ project_name }}.Example()

    # Step 2: Configure
    obj.configure(setting='value')

    # Step 3: Execute
    result = obj.run()

Pattern 2: Batch Processing
~~~~~~~~~~~~~~~~~~~~~~~~~~~~

::

    # Process multiple items
    items = ['item1', 'item2', 'item3']
    results = [{{ project_name }}.process(item) for item in items]

Troubleshooting
---------------

Common Issues
~~~~~~~~~~~~~

**Problem**: Import error

**Solution**: Make sure {{ project_name }} is installed in your Python environment.

**Problem**: Configuration not working

**Solution**: Check that your configuration file is in the correct format.

Further Reading
---------------

{% if features.api_docs -%}
- See the :doc:`api` for complete reference documentation
{% endif -%}
- Check :doc:`contributing` to learn how to contribute
- Review the :doc:`changelog` for recent changes
